# Strix is an infrastructure scan issue that is skipped right now according to guidelines (acknowledged wait state).
# But the CI checks failed from Github actions internal error which requires re-submitting PR to trigger them.
# Submit tool pushes automatically.
